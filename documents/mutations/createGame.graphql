mutation createMultiplayerGame(
  $from: LocalDate!
  $initialWallet: Int!
  $private: Boolean!
  $to: LocalDate!
  $turnDuration: Int!
) {
  createGame(
    from: $from
    to: $to
    initialWallet: $initialWallet
    private: $private
    turnDuration: $turnDuration
  )
}
