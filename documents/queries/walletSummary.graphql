query GetWalletSummary($gameId: Int!) {
    getUserWallet(gameId: $gameId){
        availableMoney
        blockedMoney
        ownedSecurities {
            ticker
            quantity
            market
        }
    }
}